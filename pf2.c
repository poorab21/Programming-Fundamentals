#include <stdio.h>
int main()
{ 
 int width=25;
 int length=22;
 int area=550;
 printf("give width:");
 scanf("%d",&width);
 printf("give length:");
 scanf("%d",&length);
 area=width*length;
 printf("the area is %d");
return 0;
}